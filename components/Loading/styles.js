import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  loading: {
    height: 80,
    flex: 1,
    alignItems: 'center',
    justifyContent: 'flex-start',
    paddingTop: 60,
  }
})